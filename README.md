# @portfolio/contracts

Shared Zod schemas, API DTOs, enums, and Azure Storage queue message unions.

## Build

```bash
npm install
npm run build
npm test
```

## Publish

Tag `contracts-v*` to trigger GitHub Actions publish to GitHub Packages.
